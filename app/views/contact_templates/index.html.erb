<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All contacts
    </h1>

    <a href="/contacts/new" class="btn btn-block btn-outline-secondary">
      Add a new contact
    </a>
  </div>
</div>

<!-- Ransack Code start -->
<div class="row">
  <div class="col-md-12 mb-3">
    <%= render partial: "contact_templates/contacts_search_form" %>
  </div>
</div>
<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          First name
        </th>

        <th>
          Last name
        </th>

        <th>
          Email
        </th>

        <th>
          User
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @contacts.each do |contact| %>
      <tr>
        <!-- Display contact.id start -->
        <td>
          <%= contact.id %>
        </td>
        <!-- Display contact.id end -->

        <!-- Display contact.first_name start -->
        <td>
          <%= contact.first_name %>
        </td>
        <!-- Display contact.first_name end -->

        <!-- Display contact.last_name start -->
        <td>
          <%= contact.last_name %>
        </td>
        <!-- Display contact.last_name end -->

        <!-- Display contact.email start -->
        <td>
          <%= contact.email %>
        </td>
        <!-- Display contact.email end -->

        <!-- Display contact.user_id start -->
        <td>
          <% if contact.user.present? %>
            <a href="/users/<%= contact.user.id %>">
              <%= contact.user.email %>
            </a>
          <% end %>
        </td>
        <!-- Display contact.user_id end -->

        <!-- Display contact.created_at start -->
        <td>
          <%= time_ago_in_words(contact.created_at) %> ago
        </td>
        <!-- Display contact.created_at end -->

        <!-- Display contact.updated_at start -->
        <td>
          <%= time_ago_in_words(contact.updated_at) %> ago
        </td>
        <!-- Display contact.updated_at end -->

        <td>
          <a href="/contacts/<%= contact.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @contacts, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
